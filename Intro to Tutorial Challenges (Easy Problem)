int introTutorial(int V, vector<int> arr) {
    sort(arr.begin(), arr.end());
    int n = arr.size();
    for(int i = 0; i < n; i++) {
        if(arr[i] == V) {
            return i;
        }
    }
    return 0;
}
